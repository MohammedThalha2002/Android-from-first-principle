---
title: "The runtime: from Kotlin to running code"
navTitle: "The runtime"
proves: "Your code is a file until the Android Runtime loads it into a process, and how it loads and compiles that file explains four bugs you have already met."
---

Not written yet. This chapter is planned in detail and its lessons are next in
the writing order.

**The lessons it will hold:** What is in your APK · ART loads your app, and why a static resets · Interpret, JIT and AOT: why the first run is slowest · The heap and the garbage collector · R8: why release breaks when debug works

The plan behind it, including what each lesson opens with and what it proves,
is in `docs/courses/android-internals/PLAN.md` in the repository.
